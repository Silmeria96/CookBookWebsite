
@{
    ViewBag.Title = "Index";
}

<style>
    .userinfo-container {
        height: 150px;
        background-color: antiquewhite;
    }

    .userinfo-head {
        width: 120px;
        height: 120px;
        margin: 15px 0;
        border-radius: 50%;
        border: 3px solid whitesmoke;
    }

    .userinfo-name {
        line-height: 120px;
    }
</style>

<div class="row">
    <div class="col-md-12" style="position:relative;">
        <div class="userinfo-container">
            <div class="row">
                <div class="col-md-2 col-md-offset-1">
                    <img class="userinfo-head" src="~/Images/Index/cookbook-1.jpg" alt="Alternate Text" />
                </div>
                <div class="col-md-6">
                    <h4 class="userinfo-name">@Session["UserDisplayName"]</h4>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row" style="margin-top:15px;">   
    <div class="col-md-2">
        <ul class="nav nav-pills nav-stacked">
            <li>@Ajax.ActionLink("个人中心", "UserInfoPartial", new AjaxOptions { UpdateTargetId = "partial-container", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })</li>
            <li>@Ajax.ActionLink("修改密码", "UserPasswordPartial", new AjaxOptions { UpdateTargetId = "partial-container", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })</li>
            <li>@Ajax.ActionLink("我的收藏", "UserCollectionPartial", new AjaxOptions { UpdateTargetId = "partial-container", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })</li>
            <li>@Ajax.ActionLink("我的订单", "UserOrderPartial", new AjaxOptions { UpdateTargetId = "partial-container", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })</li>
            <li>@Ajax.ActionLink("我的文章", "UserArticlePartial", new AjaxOptions { UpdateTargetId = "partial-container", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })</li>
            <li>@Html.ActionLink("退出","Logout","Account")</li>
        </ul>
    </div>
    <div class="col-md-9">
        <div class="panel panel-default">
            <div class="panel-body">
                <div id="partial-container">
                    欢迎来到个人中心！
                </div>
            </div>
        </div>
    </div>
</div>
